os: Visual Studio 2015

matrix:
  fast_finish: true

platform:
  - x86
  - x64

configuration:
  - Release

before_build:
  - git submodule update --init --recursive
  - python get_steinberg_sdk.py
  - cmake -G "Visual Studio 14" --debug-output Smexoscope

build:
  project: Smexoscope.sln
