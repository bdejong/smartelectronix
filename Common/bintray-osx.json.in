{
    "package": {
        "name": "OSX",
        "repo": "Plugins",
        "subject": "bdejong"
    },

    "version": {
            "name": "1.0.$ENV{TRAVIS_BUILD_NUMBER}",
            "desc": "Latest builds",
            "gpgSign": false
        },

    "files":
        [
            {"includePattern": "OSX-Plugins-1.0.$ENV{TRAVIS_BUILD_NUMBER}.zip", "uploadPattern": "OSX-Plugin-1.0.$ENV{TRAVIS_BUILD_NUMBER}.zip"}
        ],

    "publish": true
}
