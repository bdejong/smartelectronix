{
    "package": {
        "name": "OSX",
        "repo": "Plugins",
        "subject": "bdejong"
    },

    "version": {
            "name": "$ENV{TRAVIS_BUILD_NUMBER}",
            "desc": "Latest builds",
            "gpgSign": false
        },

    "files":
        [
            {"includePattern": "OSX-Plugins-$ENV{TRAVIS_BUILD_NUMBER}.zip", "uploadPattern": "OSX-Plugin-$ENV{TRAVIS_BUILD_NUMBER}.zip"}
        ],

    "publish": true
}
